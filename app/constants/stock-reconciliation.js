'use strict';

const STOCK_RECONCILIATION_MODULE = 'STOCK_RECONCILIATION';

const STOCK_RECONCILIATION_PERMISSION_ACTIONS = Object.freeze({
  read: 'view',
  investigate: 'create',
  regularize: 'update',
  authorize: 'reports',
});

const STOCK_RECONCILIATION_STATUSES = Object.freeze({
  DETECTED: 'DETECTADA',
  INVESTIGATING: 'EN_INVESTIGACION',
  READY: 'LISTA_PARA_REGULARIZAR',
  RESOLVED: 'RESUELTA',
});

const STOCK_RECONCILIATION_STRATEGIES = Object.freeze({
  ADJUST_BOTH_TO_PHYSICAL_COUNT: 'AJUSTAR_AMBOS_AL_CONTEO_FISICO',
  REGISTER_MISSING_KARDEX: 'REGISTRAR_KARDEX_OMITIDO',
  ADJUST_STOCK_BY_COUNT: 'AJUSTAR_STOCK_POR_CONTEO',
  LINK_EXISTING: 'VINCULAR_REGULARIZACION_EXISTENTE',
  CONTINUE_INVESTIGATION: 'CONTINUAR_INVESTIGACION',
});

module.exports = {
  STOCK_RECONCILIATION_MODULE,
  STOCK_RECONCILIATION_PERMISSION_ACTIONS,
  STOCK_RECONCILIATION_STATUSES,
  STOCK_RECONCILIATION_STRATEGIES,
};
