const PRODUCT_CATEGORY_TYPES = Object.freeze([
  'RAW_MATERIAL',
  'FINISHED_PRODUCT',
  'RESALE_ITEM',
]);

const PRODUCT_ACCESS_CONTEXTS = Object.freeze({
  PURCHASES: 'COMPRAS',
  SALES: 'VENTAS',
  TRANSFERS: 'TRASLADOS',
  CLASSIFIEDS: 'CLASIFICADOS',
});

const PRODUCT_ACCESS_MODULES = Object.freeze(Object.values(PRODUCT_ACCESS_CONTEXTS));

const PRODUCT_ACCESS_ROUTE_CONTEXTS = Object.freeze({
  purchases: PRODUCT_ACCESS_CONTEXTS.PURCHASES,
  sales: PRODUCT_ACCESS_CONTEXTS.SALES,
  transfers: PRODUCT_ACCESS_CONTEXTS.TRANSFERS,
  classifieds: PRODUCT_ACCESS_CONTEXTS.CLASSIFIEDS,
});

module.exports = {
  PRODUCT_CATEGORY_TYPES,
  PRODUCT_ACCESS_CONTEXTS,
  PRODUCT_ACCESS_MODULES,
  PRODUCT_ACCESS_ROUTE_CONTEXTS,
};
